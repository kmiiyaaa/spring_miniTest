<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
   
<mapper namespace="com.kmi.miniTest.dao.MemberDao">

<insert id="memberJoinDao">
	INSERT INTO member(memberid, userid, memberpw, membername) 
	VALUES (member_seq.nextval, #{param1}, #{param2}, #{param3})
</insert>

<select id="memberidCheckDao" resultType="int">
	SELECT COUNT(*) FROM member WHERE memberid=#{param1}  <!-- 아이디가 있으면 1반환  -->
</select>








</mapper>