<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
   
   
<mapper namespace="com.kmi.miniTest.dao.MemberDao">

<insert id="memberJoinDao">
	INSERT INTO member(userid, memberid, memberpw, membername) 
	VALUES (member_seq.nextval, #{param1}, #{param2}, #{param3})
</insert>

<select id="memberidCheckDao" resultType="int"> <!-- 아이디가 있으면 1반환  -->
	SELECT COUNT(*) FROM member WHERE memberid=#{param1}  
</select>

<select id="memberLoginDao" resultType="int"> <!-- 아이디가 있으면 1반환  -->
	SELECT COUNT(*) FROM member WHERE memberid=#{param1} AND memberpw=#{param2}
</select>








</mapper>